This directory will store the Jupyter notebooks or Julia scripts used to create the new products.

## File names

Indicate the region name in the file name containing the code, i.e., avoid names such as `run.jl` or `climatology.jl`.


